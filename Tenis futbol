```html
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Football Zone | Tenis de Fútbol</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, Helvetica, sans-serif;
        }

        body {
            background-color: #f5f5f5;
            color: #222;
        }

        /* NAVBAR */
        header {
            background-color: #111;
            color: white;
            padding: 18px 7%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .logo {
            font-size: 25px;
            font-weight: bold;
        }

        .logo span {
            color: #00c853;
        }

        nav a {
            color: white;
            text-decoration: none;
            margin-left: 25px;
            font-size: 15px;
        }

        nav a:hover {
            color: #00c853;
        }

        .cart-button {
            background-color: #00c853;
            border: none;
            color: white;
            padding: 10px 15px;
            border-radius: 6px;
            cursor: pointer;
            font-weight: bold;
        }

        /* HERO */
        .hero {
            background: linear-gradient(rgba(0,0,0,.6), rgba(0,0,0,.6)),
            url("https://images.unsplash.com/photo-1579952363873-27f3bade9f55?auto=format&fit=crop&w=1600&q=80");

            background-size: cover;
            background-position: center;
            height: 430px;

            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
        }

        .hero-content h1 {
            font-size: 48px;
            margin-bottom: 15px;
        }

        .hero-content p {
            font-size: 19px;
            margin-bottom: 25px;
        }

        .hero-button {
            background-color: #00c853;
            color: white;
            padding: 13px 25px;
            border: none;
            border-radius: 6px;
            text-decoration: none;
            font-weight: bold;
        }

        /* PRODUCTOS */
        .container {
            width: 86%;
            max-width: 1200px;
            margin: 50px auto;
        }

        .section-title {
            text-align: center;
            margin-bottom: 30px;
        }

        .section-title h2 {
            font-size: 32px;
        }

        .section-title p {
            color: #666;
            margin-top: 8px;
        }

        /* FILTROS */
        .filters {
            display: flex;
            justify-content: space-between;
            gap: 15px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .filters input,
        .filters select {
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 6px;
            background: white;
        }

        .filters input {
            width: 250px;
        }

        /* GRID */
        .products {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 25px;
        }

        .product {
            background-color: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 3px 12px rgba(0,0,0,.08);
            transition: .3s;
        }

        .product:hover {
            transform: translateY(-5px);
        }

        .product img {
            width: 100%;
            height: 230px;
            object-fit: cover;
        }

        .product-info {
            padding: 18px;
        }

        .brand {
            color: #777;
            font-size: 13px;
        }

        .product h3 {
            margin: 7px 0;
            font-size: 20px;
        }

        .description {
            color: #666;
            font-size: 14px;
            margin-bottom: 15px;
        }

        .price {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 15px;
        }

        .add-button {
            width: 100%;
            padding: 11px;
            background-color: #111;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .add-button:hover {
            background-color: #00c853;
        }

        /* CARRITO */
        .cart {
            position: fixed;
            right: -400px;
            top: 0;
            width: 360px;
            height: 100%;
            background: white;
            z-index: 200;
            padding: 25px;
            box-shadow: -4px 0 15px rgba(0,0,0,.2);
            transition: .3s;
            overflow-y: auto;
        }

        .cart.active {
            right: 0;
        }

        .cart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }

        .close-cart {
            background: none;
            border: none;
            font-size: 25px;
            cursor: pointer;
        }

        .cart-item {
            border-bottom: 1px solid #ddd;
            padding: 15px 0;
        }

        .cart-item h4 {
            margin-bottom: 7px;
        }

        .remove {
            background: #e53935;
            color: white;
            border: none;
            padding: 5px 9px;
            border-radius: 4px;
            cursor: pointer;
            margin-top: 8px;
        }

        .cart-total {
            font-size: 22px;
            font-weight: bold;
            margin: 25px 0;
        }

        .checkout {
            width: 100%;
            padding: 13px;
            border: none;
            background: #00c853;
            color: white;
            font-weight: bold;
            border-radius: 6px;
            cursor: pointer;
        }

        /* FOOTER */
        footer {
            background-color: #111;
            color: white;
            text-align: center;
            padding: 30px;
            margin-top: 60px;
        }

        footer span {
            color: #00c853;
        }

        /* RESPONSIVE */
        @media (max-width: 900px) {
            .products {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 600px) {
            header {
                flex-direction: column;
                gap: 15px;
            }

            nav a {
                margin: 0 7px;
            }

            .hero-content h1 {
                font-size: 34px;
            }

            .products {
                grid-template-columns: 1fr;
            }

            .filters input {
                width: 100%;
            }

            .cart {
                width: 100%;
            }
        }
    </style>
</head>

<body>

    <!-- NAVBAR -->
    <header>
        <div class="logo">
            Football<span>Zone</span>
        </div>

        <nav>
            <a href="#inicio">Inicio</a>
            <a href="#productos">Tenis</a>
            <a href="#contacto">Contacto</a>
            <button class="cart-button" onclick="openCart()">
                🛒 Carrito (<span id="cart-count">0</span>)
            </button>
        </nav>
    </header>

    <!-- HERO -->
    <section class="hero" id="inicio">
        <div class="hero-content">
            <h1>Juega a otro nivel</h1>
            <p>Encuentra los tenis de fútbol ideales para tu estilo de juego.</p>
            <a href="#productos" class="hero-button">
                Ver tenis
            </a>
        </div>
    </section>

    <!-- PRODUCTOS -->
    <main class="container" id="productos">

        <div class="section-title">
            <h2>Tenis de fútbol</h2>
            <p>Elige tus próximos tenis y prepárate para jugar.</p>
        </div>

        <!-- FILTROS -->
        <div class="filters">

            <input
                type="text"
                id="search"
                placeholder="Buscar tenis..."
                onkeyup="filterProducts()"
            >

            <select id="brandFilter" onchange="filterProducts()">
                <option value="all">Todas las marcas</option>
                <option value="Nike">Nike</option>
                <option value="Adidas">Adidas</option>
                <option value="Puma">Puma</option>
            </select>

        </div>

        <!-- PRODUCTOS -->
        <div class="products" id="product-list">

            <div class="product" data-brand="Nike" data-name="Mercurial Vapor">
                <img src="https://images.unsplash.com/photo-1511886929837-354d827aae26?auto=format&fit=crop&w=800&q=80">

                <div class="product-info">
                    <span class="brand">Nike</span>
                    <h3>Mercurial Vapor</h3>

                    <p class="description">
                        Tenis ligeros diseñados para velocidad y aceleración.
                    </p>

                    <div class="price">$2,499 MXN</div>

                    <button class="add-button"
                        onclick="addToCart('Nike Mercurial Vapor', 2499)">
                        Agregar al carrito
                    </button>
                </div>
            </div>


            <div class="product" data-brand="Adidas" data-name="Predator Elite">
                <img src="https://images.unsplash.com/photo-1517466787929-bc90951d0974?auto=format&fit=crop&w=800&q=80">

                <div class="product-info">
                    <span class="brand">Adidas</span>
                    <h3>Predator Elite</h3>

                    <p class="description">
                        Diseñados para mejorar el control y precisión del balón.
                    </p>

                    <div class="price">$2,799 MXN</div>

                    <button class="add-button"
                        onclick="addToCart('Adidas Predator Elite', 2799)">
                        Agregar al carrito
                    </button>
                </div>
            </div>


            <div class="product" data-brand="Puma" data-name="Future Ultimate">
                <img src="https://images.unsplash.com/photo-1553778263-73a83bab9b0c?auto=format&fit=crop&w=800&q=80">

                <div class="product-info">
                    <span class="brand">Puma</span>
                    <h3>Future Ultimate</h3>

                    <p class="description">
                        Comodidad y movilidad para jugadores creativos.
                    </p>

                    <div class="price">$2,599 MXN</div>

                    <button class="add-button"
                        onclick="addToCart('Puma Future Ultimate', 2599)">
                        Agregar al carrito
                    </button>
                </div>
            </div>


            <div class="product" data-brand="Adidas" data-name="Copa Pure">
                <img src="https://images.unsplash.com/photo-1551958219-acbc608c6377?auto=format&fit=crop&w=800&q=80">

                <div class="product-info">
                    <span class="brand">Adidas</span>
                    <h3>Copa Pure</h3>

                    <p class="description">
                        Un modelo clásico enfocado en comodidad y control.
                    </p>

                    <div class="price">$2,299 MXN</div>

                    <button class="add-button"
                        onclick="addToCart('Adidas Copa Pure', 2299)">
                        Agregar al carrito
                    </button>
                </div>
            </div>


            <div class="product" data-brand="Nike" data-name="Phantom GX">
                <img src="https://images.unsplash.com/photo-1579952363873-27f3bade9f55?auto=format&fit=crop&w=800&q=80">

                <div class="product-info">
                    <span class="brand">Nike</span>
                    <h3>Phantom GX</h3>

                    <p class="description">
                        Gran agarre y precisión para atacar.
                    </p>

                    <div class="price">$2,699 MXN</div>

                    <button class="add-button"
                        onclick="addToCart('Nike Phantom GX', 2699)">
                        Agregar al carrito
                    </button>
                </div>
            </div>


            <div class="product" data-brand="Puma" data-name="Ultra Ultimate">
                <img src="https://images.unsplash.com/photo-1431324155629-1a6deb1dec8d?auto=format&fit=crop&w=800&q=80">

                <div class="product-info">
                    <span class="brand">Puma</span>
                    <h3>Ultra Ultimate</h3>

                    <p class="description">
                        Modelo ligero pensado para jugadores rápidos.
                    </p>

                    <div class="price">$2,399 MXN</div>

                    <button class="add-button"
                        onclick="addToCart('Puma Ultra Ultimate', 2399)">
                        Agregar al carrito
                    </button>
                </div>
            </div>

        </div>
    </main>


    <!-- CARRITO -->
    <aside class="cart" id="cart">

        <div class="cart-header">
            <h2>Tu carrito</h2>

            <button class="close-cart" onclick="closeCart()">
                ×
            </button>
        </div>

        <div id="cart-items">
            <p>Tu carrito está vacío.</p>
        </div>

        <div class="cart-total">
            Total: $<span id="cart-total">0</span> MXN
        </div>

        <button class="checkout" onclick="checkout()">
            Finalizar compra
        </button>

    </aside>


    <!-- FOOTER -->
    <footer id="contacto">
        <p>© 2026 <span>FootballZone</span> | Todos los derechos reservados.</p>
        <p>Tu pasión. Tu juego. Tus tenis.</p>
    </footer>


    <script>

        // CARRITO
        let cart = [];

        function addToCart(name, price) {

            cart.push({
                name: name,
                price: price
            });

            updateCart();

            alert(name + " fue agregado al carrito.");
        }


        function updateCart() {

            const cartItems = document.getElementById("cart-items");
            const cartCount = document.getElementById("cart-count");
            const cartTotal = document.getElementById("cart-total");

            cartItems.innerHTML = "";

            let total = 0;

            if (cart.length === 0) {

                cartItems.innerHTML =
                    "<p>Tu carrito está vacío.</p>";

            } else {

                cart.forEach((item, index) => {

                    total += item.price;

                    cartItems.innerHTML += `
                        <div class="cart-item">

                            <h4>${item.name}</h4>

                            <p>$${item.price.toLocaleString()} MXN</p>

                            <button
                                class="remove"
                                onclick="removeFromCart(${index})">
                                Eliminar
                            </button>

                        </div>
                    `;

                });

            }

            cartCount.textContent = cart.length;

            cartTotal.textContent =
                total.toLocaleString();
        }


        function removeFromCart(index) {

            cart.splice(index, 1);

            updateCart();
        }


        function openCart() {

            document
                .getElementById("cart")
                .classList.add("active");

        }


        function closeCart() {

            document
                .getElementById("cart")
                .classList.remove("active");

        }


        function checkout() {

            if (cart.length === 0) {

                alert("Tu carrito está vacío.");

                return;
            }

            alert(
                "¡Gracias por tu compra! ⚽\n\n" +
                "Tu pedido ha sido registrado."
            );

            cart = [];

            updateCart();

            closeCart();
        }


        // BUSCADOR Y FILTRO
        function filterProducts() {

            const search =
                document
                .getElementById("search")
                .value
                .toLowerCase();

            const brand =
                document
                .getElementById("brandFilter")
                .value;

            const products =
                document.querySelectorAll(".product");


            products.forEach(product => {

                const name =
                    product.dataset.name.toLowerCase();

                const productBrand =
                    product.dataset.brand;


                const matchesSearch =
                    name.includes(search);

                const matchesBrand =
                    brand === "all" ||
                    productBrand === brand;


                if (matchesSearch && matchesBrand) {

                    product.style.display = "block";

                } else {

                    product.style.display = "none";

                }

            });

        }

    </script>

</body>
</html>
```

